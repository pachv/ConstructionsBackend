CREATE TABLE ask_questions (
    id VARCHAR(255),
    message VARCHAR(255),
    name VARCHAR(255),
    phone VARCHAR(255),
    email VARCHAR(255),
    product VARCHAR(255),
    consent BOOLEAN,
    created_at TIMESTAMP
);